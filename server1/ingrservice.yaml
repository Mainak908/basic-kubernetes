apiVersion: v1
kind: Service
metadata:
  name: node-app-server1-service
spec:
  selector:
    app: node-app-server1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
